{
  "name": "labtrack",
  "version": "1.0.0",
  "description": "Laboratory Consumables Management System",
  "scripts": {
    "install:all": "cd backend && python -m venv venv && venv\\Scripts\\python -m pip install -r requirements.txt && cd ../frontend && npm install",
    "dev:backend": "cd backend && venv\\Scripts\\python -m uvicorn app.main:app --host 0.0.0.0 --port 5000 --reload",
    "dev:frontend": "cd frontend && npm run dev",
    "db:setup": "cd backend && venv\\Scripts\\python -m app.scripts.migrate && venv\\Scripts\\python -m app.scripts.seed"
  }
}
